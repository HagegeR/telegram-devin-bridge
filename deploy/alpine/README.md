# Alpine and VM deployment

The installer moved to `deploy/vm`. The compatibility shim remains available:

```sh
sh deploy/vm/install.sh
# or:
sh deploy/alpine/install.sh
```

See the main README for setup, upgrades, service commands, and Fly database
migration instructions.
